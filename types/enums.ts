export enum PaymentStatus {
    PAYED = "PAYED",
    PENDING = "PENDING",
    CANCELLED = "CANCELLED",
    DECLINED = "DECLINED"
}

export enum OrderStatus {
    PENDING = "PENDING",
    DELIVERED = "DELIVERED",
    CANCELLED = "CANCELLED",
}